# Test-only dependencies. Not installed into the image (Dockerfile uses
# requirements.txt), so nothing here ships to production.
-r requirements.txt
pytest>=8.0
mongomock-motor>=0.0.29
httpx>=0.27          # starlette.testclient transport
