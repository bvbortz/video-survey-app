[pytest]
testpaths = tests
# The tests import the application as `app`, which is a package at the repo root
# rather than an installed distribution. `python -m pytest` happens to work
# because it puts the cwd on sys.path, but bare `pytest` (what CI runs) does not,
# so the import failed there and only there. Setting it here makes both work.
pythonpath = .
